spring.application.name=order-service

server.port=8053

spring.datasource.url=jdbc:postgresql://localhost:5432/ecomOrderMs
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto= update
spring.jpa.show-sql=true

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

management.endpoints.web.exposure.include=*

##product.service.url=http://localhost:8051/products/exists/{productId}
#product.service.url=http://product-service/products/exists/{productId}
#user.service.url=http://user-service/users/exists/{userId}

eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.ip-address=true